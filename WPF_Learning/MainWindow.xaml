<Window x:Class="WPF_Learning.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Learning"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid Background="DarkSlateBlue">
        <!-- Definicja Wierszy i wiersz -->
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <!-- * oznacza, że eleenty mają proporcjonalne długości, z pozostałych do wykorzystania -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <!-- Auto dopasowuje rozmiar do wymiarów obiektu który się znajduje w tym wierszu i kolumnie-->
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button x:Name="add" Click="Add_Click" Margin="15" Padding="10">
            <Image Grid.Column="0" Source="images\base_image.png"/>
        </Button>
        <Label Grid.Column="1" x:Name ="label_new" Content="Label" Background ="AliceBlue" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock x:Name="textBlock" Padding="5" FontSize="12" FontFamily="Comic Sans MS" Margin="10,10,10,10" Grid.Row="1" Grid.ColumnSpan="2" Background="Silver">

        </TextBlock>
    </Grid>
</Window>
